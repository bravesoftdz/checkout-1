CREATE TABLE PRODUTOCOMPOSICAO
(
  PRODICOD  	INTEGER NOT NULL,
  PRODICODFILHO INTEGER NOT NULL,
  PRODN3QTDE	NUMERIC(15, 3),
  PENDENTE	CHAR(1),
  REGISTRO	TIMESTAMP,
  PRIMARY KEY (PRODICOD,PRODICODFILHO)
);


CREATE UNIQUE INDEX XPKPRODUTOCOMPOSICAO ON PRODUTOCOMPOSICAO
(
       PRODICOD,
       PRODICODFILHO
);